function getAdmins(map) {
    let admins = [];
    for([sio, value] of map){
        if (value === 'admi') {admins.push(cao)
        
        }
    }  
    return admins;    
  
}
const usuario = new map();

usuario = ('cassio', 'admi');
usuario = ('iiit', 'admi');
usuario = ('sio', 'user');
usuario = ('cao', 'user');
console.log(getAdmins(usuario));